# 📋 Project TODO

## 📅 2026-03-13

### 1. ✅ 완료된 작업 (기분/활동 선택 모달 개선)
- [x] **모달 밖 터치 시 상태 복구**: [선택하기]를 누르지 않고 모달을 닫을 경우(배경 터치, 스와이프 등), 변경된 내용을 무시하고 이전 선택 상태로 복원하는 기능 구현.
- [x] **하드웨어 뒤로가기 버튼 연동**: 안드로이드 하드웨어 뒤로가기 버튼을 눌렀을 때도 초기화 로직이 동일하게 작동하도록 `onBackButtonPress` 추가.
- [x] **신규 일기 작성 시 상태 복구 및 자동 저장 방지**: 기분이 선택되지 않은 첫 진입 상태에서 모달을 닫을 때, 임시 선택값이 자동 저장되지 않도록 `latestData` Ref 강제 업데이트 및 `navigation.goBack()` 처리.
- [x] **기분 선택 강제(Validation)**: 기분을 선택하지 않고 [선택하기] 버튼을 누를 경우, 알림창(SoftAlert)을 띄우고 모달이 닫히지 않도록 방어 로직 추가.

### 2. 🔍 성능 분석 (연간 요약 화면 로딩 속도)
- **요청 내역**: "올해의 단어" 추가 후 로딩이 느려진 원인 분석.
- **분석 결과**: 
    - "올해의 단어" 자체는 전용 통계 테이블과 증분 동기화(`word_stats`)를 사용하여 최적화가 잘 되어 있음.
    - **실제 병목 원인**: 황금 시간대(Golden Hour) 분석, 기분x활동 상관관계 계산 등이 DB가 아닌 메모리(JS 루프)에서 매번 올해의 전체 일기를 대상으로 실행되고 있음.
    - 특히 일기 개수가 수백 개 단위로 늘어나면, `SummaryScreen` 진입 시 발생하는 여러 대형 쿼리와 무거운 연산이 하드웨어 부하를 유발함.

### 3. 🚀 추후 개선 과제 (성능 최적화)
- [ ] **활동/시간 통계 테이블화**: "올해의 단어"처럼 기분/활동 상관관계 및 시간대별 통계도 별도의 통계 테이블에 캐싱하여 Query 속도 개선.
- [ ] **데이터 로딩 시점 분산**: 연간 요약 화면 진입 시 모든 데이터를 한꺼번에 로드하지 않고, 현재 보이는 탭의 데이터부터 순차적으로 로딩.
- [ ] **heavy 연산 Web Worker(또는 유사 방식) 검토**: 대용량 데이터 분석 시 UI 스레드 차단을 방지하기 위한 로직 분리.
- [ ] **차트 렌더링 최적화**: 데이터 가공 로직에 `useMemo`를 더 촘촘하게 적용하여 불필요한 리렌더링 및 재계산 방지.
